const express = require('express')
const router = express.Router()
const Destinations = require('../models/destinations')

router.get('/', (req, res) => {
    app.send('say hello MARIANA')
})
module.exports = router